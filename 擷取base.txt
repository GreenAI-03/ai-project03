  <style>
        [v-cloak] {
            display: none;
        }
        .product-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
            gap: 16px;
        }
        .title-bar {
            background-color: #1976D2 !important;
        }
        .menu {
            background-color: #e0f2f1 !important;
            display: flex;
            flex-direction: column;
            height: 100%;
            padding-bottom: 64px; /* Leave space for the login button */
        }
        .login-register-btns {
            margin-top: auto;
            margin-bottom: 20px;
            position: absolute;
            bottom: 0;
            display: flex;
            justify-content: space-around;
            width: 100%;
            padding: 0 10px;
        }
        .common-btn {
            background-color: #1976D2 !important; /* Same background color for both buttons */
            color: #FFF !important; /* White text color for both buttons */
        }
    </style>